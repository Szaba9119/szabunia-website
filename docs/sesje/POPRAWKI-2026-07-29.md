# Raport — Audyt pełny + cennik v3 (2026-07-29)

**Status:** DONE (kod), PARTIAL (całość — 6 findingów czeka na decyzje D1–D10 z raportu audytu)

Punkt wyjścia: `645e49c` = `origin/main` = produkcja. **Nie commitowałem i nie pushowałem.**
Zmiany leżą w drzewie roboczym, do przejrzenia i wypchnięcia przez Ciebie.

---

## Co zrobione

### A. Cennik v3 — tylko tam, gdzie ceny już były (Twoja decyzja z 29.07)

- **7 kotwic „od X zł" w kartach usług** (`services.tsx`): portrety 1 000→**1 100**, pakiety hybrydowe 1 800→**2 100**, wideo 300→**400**, dron 900→**700**, produktowa 500→**600**, sesje zespołowe 150→**120 zł/os.**, eventy **600 bez zmian**.
- **Pełna siatka cen w `llms.txt`** przepisana na v3: nazwy pakietów **STANDARD / PRO / PRO MAX** (portrety 1 100 / 1 400 / 1 800, eventy 2 100 / 3 600 / 5 000 z zakresami), reportaż 600 + **350** za kolejną godzinę, pakiet 4 h **1 500**, całodniowy **2 600**, sesje zespołowe **180 / 150 / 120** + dodatkowe ujęcie 100 zł + wynajem studia 400 / 800 / 1 300, packshot min. zamówienie **600 zł**, wideo: teaser 300 (tylko jako dodatek), **montaż wywiadu 450 (nowa pozycja)**, Reels **1 200**, recap **1 800**, Promo i Dokument **wycena indywidualna**, Monthly Content 4 900 zł/m-c bez zmian.
- **Rozróżnienie montaż vs operator** dopisane w `llms.txt` i w blogu: pakiet montażowy to sama postprodukcja; jeśli materiał nagrywam ja, dochodzi praca operatora.
- **Wszystkie kwoty we wpisach blogowych** wyrównane do v3 (event, wideo, portrety). Dron bez zmian — v3 potwierdza dotychczasowe stawki.
- **FAQ na home:** „Sesje portretowe zaczynają się od 1 000 zł" → **1 100 zł**.

### B. Terminy i poprawki (sekcja 3 briefu)

- Słowo **„roboczych" usunięte ze wszystkich 12 miejsc**: `services.tsx` (3×), `faq.ts`, `Warunki.tsx`, `portfolio.ts` (2×), `blog.ts` (2×), `llms.txt` (2×). Jedna jednostka w całej komunikacji: dni kalendarzowe. Zdjęcia 14 dni, wideo 21 dni.
- **Dopisane zdanie o poprawkach** (`faq.ts`, `llms.txt`): „Poprawki realizuję w ciągu 7 dni od zgłoszenia. Nie liczę terminu podstawowego od nowa."
- **Dopisana definicja tury**: „Tura poprawek to korekta w obrębie wybranego materiału. Zmianę wyboru kadrów po rozpoczęciu retuszu wyceniam osobno i zawsze o tym uprzedzam."

### C. Liczba zdjęć z eventu (sekcja 4 briefu)

- `services.tsx` i `portfolio.ts`: „15-30 zdjęć na godzinę" → **„około 20 gotowych zdjęć na każdą godzinę obecności"** + zdanie, że przy pakietach foto + wideo jest ich mniej, bo część czasu idzie na nagrywanie.

### D. Poprawki z audytu (ID z `AUDYT-PELNY-2026-07-29.md`)

| ID | Co zmienione |
|---|---|
| `PELNY2907-02` | **15 odesłań do kalkulatora usunięte z bloga.** `grep -rn "kalkulator" src/ public/` → **0**. Teksty przepisane na „napisz, czego potrzebujesz" |
| `PELNY2907-06` | `llms.txt`: „Usługi i cennik" → „Usługi", „Kontakt i kalkulator wyceny" → „Kontakt" |
| `PELNY2907-07` | Zgoda RODO wysyłana na serwer (`consent`, `consentText`, `consentTs`), walidowana serwerowo (400 bez zgody), dopisywana do maila i do CRM. Treść klauzuli w jednej stałej `CONSENT_TEXT` |
| `PELNY2907-09` | `FAQ.tsx` renderuje **wszystkie 16** pozycji i chowa nadmiar CSS-em zamiast `slice` — parytet z JSON-LD `FAQPage` |
| `PELNY2907-12` | Żargon „brief" zdjęty z powierzchni lejka: `/kontakt` (nagłówek karty, lead, meta OG i Twitter, CTA pod ofertą), `Process.tsx` („Brief" → „Rozmowa"), `About.tsx`, `/uslugi`, `PRICE_FAQ_CLOSING` w `services.tsx` (jedna zmiana = FAQ 7 usług) |
| `PELNY2907-14` | `pushToCrm` **przed** sprawdzeniem odpowiedzi Resend — awaria maila nie kasuje już leada z obu kanałów naraz |
| `PELNY2907-15` | `/api/lead`: najpierw powiadomienie do Ciebie i zapis do CRM, potem mail z poradnikiem. Odbicie maila do subskrybenta **nie** zwraca już 502, tylko log `[ALERT]` |
| `PELNY2907-21` | Pole `service` walidowane względem listy kodów usług (400 przy nieznanym) |
| `PELNY2907-22` | Link „polityką prywatności" w klauzuli RODO: `dark:text-blue-light` — kontrast w trybie ciemnym z 3,32:1 na ok. 5,3:1 |
| `PELNY2907-24` | `<PoradnikForm />` opakowany w `<ErrorBoundary>` |
| `PELNY2907-25` | Usunięty martwy limiter `quoteRatelimit` / `isQuoteRateLimited` po skasowanym `/api/quote` |
| `PELNY2907-26` | Fail-open rate-limitu loguje się z markerem `[ALERT]`, tak jak Turnstile |
| `PELNY2907-29` | `sitemap.ts`: `lastModified` na **wszystkich** trasach (było 26 z 46), przez stałą `SITE_UPDATED` podnoszoną ręcznie |
| `PELNY2907-33` | `robots.txt`: `Disallow` na PDF poradnika — lead magnet ma być bramkowany formularzem, nie wyszukiwarką |
| `PELNY2907-35` | „Rozstawienie zajmuje około 30 minut" → **20 minut** (kanon z `services.tsx`, 8 innych miejsc mówiło 20) |
| `PELNY2907-37` | Ten sam cytat opinii w dwóch wersjach → ujednolicony (długi myślnik na przecinek). „Kompleksowe usługi foto i wideo dla firm" → „Foto, wideo i dron dla firm, z jednego wejścia" (fraza z czarnej listy `zasady-tekstow.md`) |

---

## Pliki zmienione

| Plik | Co się stało |
|---|---|
| `src/data/services.tsx` | 7 kotwic cenowych v3, terminy bez „roboczych", 20 zdjęć/h, `PRICE_FAQ_CLOSING` bez żargonu, komentarz o cenniku dronowym |
| `src/data/blog.ts` | Wszystkie kwoty v3 (event, wideo, portrety), 15 odesłań do kalkulatora przepisanych, 30→20 minut rozstawienia |
| `public/llms.txt` | Pełna siatka cen v3, nazwy PRO / PRO MAX, warunki (proforma, znak wodny), terminy kalendarzowe, etykiety bez kalkulatora |
| `src/data/faq.ts` | 1 100 zł, terminy bez „roboczych", zdanie o 7 dniach na poprawki + definicja tury, „brief" → „rozmowa" |
| `src/data/portfolio.ts` | Terminy bez „roboczych" (2×), 20 zdjęć/h |
| `src/components/Warunki.tsx` | Terminy bez „roboczych" |
| `src/components/FAQ.tsx` | Renderuje wszystkie pozycje, nadmiar chowany CSS-em (parytet z JSON-LD) |
| `src/components/CTA.tsx` | Dowód zgody RODO wysyłany na serwer, stała `CONSENT_TEXT`, kontrast linku w trybie ciemnym |
| `src/components/Testimonials.tsx` | Cytat opinii ujednolicony z wersją w `services.tsx` |
| `src/components/Services.tsx` | Podtytuł bez frazy z czarnej listy |
| `src/components/Process.tsx` | Krok „Brief" → „Rozmowa" |
| `src/components/About.tsx` | „od briefu do dostawy" → „od pierwszej rozmowy do dostawy" |
| `src/app/kontakt/page.tsx` | Cztery powierzchnie bez żargonu „brief" (w tym meta OG i Twitter) |
| `src/app/uslugi/page.tsx` | „od briefu do dostawy" → „od rozmowy do dostawy" |
| `src/app/poradnik/page.tsx` | `ErrorBoundary` wokół formularza + import |
| `src/app/api/contact/route.ts` | Walidacja `service`, serwerowa walidacja zgody + dowód w mailu i CRM, `pushToCrm` przed wysyłką maila |
| `src/app/api/lead/route.ts` | Odwrócona kolejność (powiadomienie + CRM przed mailem do subskrybenta), brak 502 przy odbiciu poradnika |
| `src/app/sitemap.ts` | `lastModified` na wszystkich trasach przez `SITE_UPDATED` |
| `src/lib/ratelimit.ts` | Usunięty martwy limiter `quote`, markery `[ALERT]` |
| `public/robots.txt` | `Disallow` na PDF poradnika |

**Nowe pliki:** `docs/sesje/AUDYT-PELNY-2026-07-29.md`, `docs/sesje/POPRAWKI-2026-07-29.md` (ten plik).

---

## Lint / Build

- `npx tsc --noEmit` → **PASS** (0 błędów)
- `npm run lint` → **PASS** (0 błędów, 0 ostrzeżeń)
- `npm run build` → **NIEURUCHOMIONY** — do odpalenia u Ciebie przed pushem (`CLAUDE.md §6`)

---

## Problemy i decyzje (stop-conditions)

### 1. Brief cennika v3 był pisany pod wersję strony sprzed 23.07

Trzy punkty briefu odnoszą się do rzeczy, których na stronie **nie ma**:

- **§6 „Kalkulator wyceny — priorytet".** `/kalkulator` nie istnieje od 23.07: jest 301 na `/kontakt`, w repo nie ma `src/app/kalkulator/` ani `PricingCalculator.tsx`. Nie było czego aktualizować.
- **§5 blok cennikowy na `/uslugi/eventy-reportaze`.** Podstrony usług nie mają tabel cenowych — została wyłącznie kotwica „od 600 zł" i opisowy `pricingBlurb`. Kwoty 1 600 / 2 800 / 3 400 żyły w `llms.txt` i we wpisach bloga i tam je poprawiłem.
- **§5 „Przez sześć lat dokumentowałem wydarzenia".** W kodzie od dawna jest **„Przez ponad osiem lat"** (`services.tsx:151`). Nic do zmiany.
- **§8 „płatnikiem VAT".** `grep -rni "płatnik" src/ public/` → **0 trafień**. Zdanie zostało usunięte commitem `ac7cf9b` 28.07, a poprawna wersja o Useme jest już w `faq.ts:32` i w `llms.txt`.

### 2. Zdanie „po wciągnięciu pełnego cennika liczba zapytań wzrosła kilkukrotnie" nie ma pokrycia w danych

Potraktowane jako pomyłka drugiego agenta (Twoja decyzja z 29.07). Dla porządku, stan faktyczny z paneli:
GA4 za ostatnie 28 dni — **39 użytkowników (−33,9%)**, `contact_submit` = **2**; cennik został ze strony **zdjęty** 23.07.
Jeśli zapytania faktycznie wzrosły, przychodzą kanałem, którego GA4 nie widzi (telefon, mail bezpośredni, Instagram DM) — wtedy daj znać, dopiszę korektę do raportu audytu.

### 3. Sekcja 2 briefu podaje ceny, których strona nie publikuje

Pełna siatka (PORTRET PRO 1 400, EVENT PRO MAX 5 000, montaż wywiadu 450) jest teraz w `llms.txt` i w blogu, ale **nie na stronie** — bo strona od 23.07 działa w modelu „cena na zapytanie". Zgodnie z Twoją decyzją nie odbudowywałem sekcji cennika. Jeśli chcesz ją z powrotem, to osobny brief (decyzja D3 w raporcie audytu).

### 4. Decyzje czekające na Ciebie

D1 (miniatura Box17), D2 (webhook CRM), D3 (ceny w `llms.txt`), D4 (obraz OG strony głównej), D5 (`Offer.price` w JSON-LD), D6 (jedna formuła obietnicy 24h), D7 (`DESIGN.md`), D8 (`CLAUDE.md`), D9 (statystyka LinkedIn 14x vs 21x), D10 (Facebook jako źródło linku) — pełne warianty w `AUDYT-PELNY-2026-07-29.md §12`.

### 5. Świadomie nie ruszone (stop-conditions)

`next.config.ts` (CSP, nagłówki, redirecty), `metadata` i JSON-LD w `layout.tsx`, `.gitignore`, `.env*`, `DESIGN.md`, `CLAUDE.md`, treść polityki prywatności (gotowy szkis czeka na akceptację), rozbudowa wpisów blogowych, zdjęcie `"use client"` z 14 komponentów (refactor >3 plików).

---

## Sugerowany commit message

```
feat(cennik): cennik v3 + terminy kalendarzowe + poprawki z audytu 2026-07-29

- ceny v3 w kartach usług, llms.txt i blogu (portrety 1100, pakiety 2100,
  wideo 400, dron 700, produktowa 600, zespoły 120/os.)
- nazwy pakietów STANDARD/PRO/PRO MAX, montaż wywiadu jako nowa pozycja
- terminy w dniach kalendarzowych, 7 dni na poprawki, definicja tury
- 15 odesłań do usuniętego kalkulatora przepisanych na model "na zapytanie"
- dowód zgody RODO na serwerze, walidacja pola service, CRM przed mailem
- FAQ w całości w DOM (parytet z JSON-LD), ErrorBoundary na /poradnik
- lastModified w całej sitemapie, PDF poradnika poza indeksem
```

---

## Co dalej

1. `npm run build` + przejrzenie diffa, potem push (Vercel zdeployuje sam).
2. Trzy rzeczy w panelach na 5 minut: GA4 → odznaczyć `calculator_done` jako kluczowe zdarzenie, GA4 → wyłączyć „Interakcje z formularzem" w pomiarze zaawansowanym, Resend → Domains sprawdzić status `szabunia.pl`.
3. Miniatura Box17 (`public/images/portfolio/box17/box17.jpg`) — 10 minut, odblokowuje główny hub portfolio.

*Raport: Claude, 2026-07-29. Zmiany w drzewie roboczym, bez commita.*

---

# Aneks — druga tura po weryfikacji adwersarialnej (2026-07-29)

Raport i poprawki przeszły kontrolę osobnym agentem („znajdź błędy, nie potwierdzaj").
Znalazł 10 realnych problemów w pierwszej turze. **Wszystkie naprawione**, pełny opis
w `AUDYT-PELNY-2026-07-29.md §15`.

**Co dołożone w drugiej turze:**

| Plik | Zmiana |
|---|---|
| `src/app/sitemap.ts` | `lastModified` także na stronie głównej (było 45 z 46 URL-i, nie 46) |
| `src/data/blog.ts` | Dwa ostatnie „21 dni roboczych" przy dronie → „21 dni". Osierocony nagłówek „Jak oszacować budżet w 2 minuty" → „Jak zaplanować budżet". „Jak to policzyć dla siebie" → „Jak to wygląda przy Twoim wydarzeniu". Usunięte wracające „napisz brief". Meta description bez „oszacuj koszt". Trzy długie myślniki usunięte. Wcięcia HTML |
| `src/app/api/contact/route.ts` | `CONSENT_TEXT` jako stała **serwerowa** z wersją; `consentTs` generowany na serwerze (`new Date().toISOString()`), nie przepisywany z żądania. `pushToCrm` bez `await` — nie dokłada do 5 s do maila i odpowiedzi |
| `src/app/api/lead/route.ts` | **Zgoda RODO wdrożona także tutaj** (walidacja + dowód w mailu i CRM, własny `CONSENT_TEXT`). Zwraca `guideSent`, żeby front nie obiecywał maila, który się odbił |
| `src/components/PoradnikForm.tsx` | Wysyła `consent: true`. Ekran sukcesu pokazuje inne zdanie, gdy mail z linkiem nie wyszedł |
| `src/data/services.tsx` | Trzy widoczne tytuły kroku „Brief" → „Rozmowa". Usunięta alternatywa „lub 6 zdjęć" przy minimum zamówienia (cennik v3 mówi 600 zł) |
| `src/data/portfolio.ts` | Dwa widoczne tytuły kroku „Brief" → „Rozmowa" |
| `src/app/poradnik/page.tsx` | Poprawione wcięcie JSX |

**Weryfikacja po drugiej turze:** `npx tsc --noEmit` PASS · `npm run lint` PASS (0/0) ·
**`npm run build` PASS** (58 stron, 46 URL-i w sitemapie, zero błędów prerenderingu) ·
`grep -rni "kalkulator" src/ public/` → 0.

**Nowe findingi z weryfikacji — czekają na Twoją decyzję** (opisane w `AUDYT §15.3`):
`PELNY2907-41` (`priceRange: "od 300 zł"` w JSON-LD stał się nieprawdziwy po v3),
`PELNY2907-42` (kotwica „od 120 zł/os." to najgłębszy próg progresji, realne minimum to 4 × 180 = 720 zł),
`PELNY2907-43` (`escapeHtml`/`isEmail`/`pushToCrm` w dwóch kopiach),
`PELNY2907-44` (obie bariery antybotowe fail-open, brak sprawdzenia `Origin`),
`PELNY2907-45` (`lastModified` wpisów bloga to data publikacji, nie modyfikacji).

**Sprostowanie do pierwszej tury raportu:** pliki `AUDYT-PELNY-2026-07-29.md`
i `POPRAWKI-2026-07-29.md` **są** w `docs/sesje/` na Twoim dysku (wgrane przeze mnie),
ale jako pliki nieśledzone przez gita — pojawią się jako `??` w `git status`.

---

# Aneks 2 — trzecia tura po decyzjach Marcina (2026-07-29, wieczór)

**Decyzje przyjęte:**

| Kwestia | Decyzja | Co zrobiłem |
|---|---|---|
| D1 · Box17 | „nie upubliczniamy na razie" | `box17-budki-akustyczne` dopisany do `DRAFT_SLUGS` w `src/data/portfolio.ts` — kafel znika z `/portfolio`, strona dostaje `noindex, follow` i wypada z sitemapy (46 → 45 URL-i). Dane case study zostają nietknięte: po wgraniu `public/images/portfolio/box17/box17.jpg` wystarczy usunąć jedną linię |
| `calculator_done` | „do usunięcia" | **Wykonane w GA4** (Administracja → Zdarzenia → Kluczowe zdarzenia): odznaczone jako kluczowe zdarzenie. Lista kluczowych zdarzeń: 8 → 7. Samego zdarzenia GA4 nie kasuje — historia zostaje, ale przestaje być liczone jako konwersja i przestaje trafiać do Google Ads |
| Webhook CRM (D2) | „nie wiem" | Bez zmian w kodzie. `pushToCrm` zostaje, dalej jest no-op bez zmiennych w Vercel. Wraca przy następnej rozmowie |
| Wizytówka Google | „na razie bez zdjęć" | Rekomendacja zdjęta z planu. Zostaje prośba o 2–3 nowe opinie |
| Ruch z Instagrama | „to było moje konto jak wchodziłem żeby sprawdzić" | Finding `PELNY2907-05` **wycofany**, w jego miejsce nowy `PELNY2907-46` — patrz niżej |

## Nowy finding: `PELNY2907-46` · [POMIAR] GA4 nie ma filtra ruchu wewnętrznego

To najważniejsza rzecz, jaka wyszła dziś z rozmowy. Czerwcowe „49 sesji z Instagrama",
na których poprzedni audyt zbudował całą narrację o kanałach, to były wizyty własne.
Konsekwencje:

- **„Spadek ruchu o 34%" nie jest spadkiem** — to zniknięcie własnych wejść. Realny ruch
  zewnętrzny był przez cały czas na poziomie kilkunastu użytkowników miesięcznie.
- **Wszystkie historyczne liczby GA4 są zawyżone** o nieznaną wielkość. Nie da się dziś
  powiedzieć, o ile — a to znaczy, że każde porównanie „przed/po" z ostatnich dwóch miesięcy
  jest niepewne, łącznie z oceną eksperymentu z depricingiem.
- Dopóki filtr nie działa, każdy kolejny odczyt konwersji jest skażony.

**Do zrobienia (5 minut, panel GA4, nie kod):** Administracja → Zbieranie i modyfikowanie
danych → **Definiuj ruch wewnętrzny** → reguła na Twoje IP (domowe i biurowe) →
potem **Filtry danych** → „Internal Traffic" przestaw z *Testowanie* na **Aktywny**.
Mogę to ustawić w kolejnej sesji, potrzebuję tylko Twoich adresów IP.

## Weryfikacja po trzeciej turze

`npx tsc --noEmit` PASS · `npm run lint` PASS (0/0) · **`npm run build` PASS**
(58 stron, kompilacja 8,0 s). Diff: **21 plików, +240 / −155**.
